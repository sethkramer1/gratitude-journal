
<div class = "card-center">


<div class ="time_heading2">  <i class="fa fa2 fa-calendar" aria-hidden="true"></i> <%= Time.now.strftime("%A, %B %d") %>
</div>
<div class = "quote">
<span style="font-style:italic;">"<%= @quote.try(:body) %>" </span>  <br><span class ="author"><%= @quote.try(:author) %></span>
 </div>
<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= simple_form_for @post do |f| %>
  <div class ="form-heading"> What are you grateful for today? </div>

  <div class = "inspired">
<%= link_to '<i class="fa fa2 fa-lightbulb-o" aria-hidden="true"></i> Need Inspiration?'.html_safe, page_path('gratitude-tips') %>
</div>

    <%= f.input :section1, label: false, input_html: {:class => "textboxline", placeholder: "I'm grateful for..."} %>
    <%= f.input :section2, label: false, input_html: {:class => "textboxline", placeholder: "A second thing I'm grateful for..."} %>
    <%= f.input :section3, label: false, input_html: {:class => "textboxline", placeholder: "A third thing I'm grateful for..."} %>

  <div class ="form-heading"> What is one thing you want to accomplish today? </div>
    <%= f.input :goal,label: false,  input_html: {:class => "textboxline",placeholder: "My biggest goal for today is..."} %>

<div class = "spacing">
    <%= f.submit 'Submit', class: "ctas-button-3" %>
</div>
  <% end %>


<% end %>
</div>
